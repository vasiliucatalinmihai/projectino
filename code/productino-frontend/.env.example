# Backend URL as seen from the browser (through the nginx proxy)
NUXT_PUBLIC_API_BASE=http://dev-api.production.io
# Backend URL as seen from the Nuxt server (SSR) over the docker network
NUXT_API_BASE_INTERNAL=http://backend:8080
